[project]
# PyPI name is getflashlight (flashlight is squatted); import + CLI stay `flashlight`
name = "getflashlight"
version = "0.1.0"
description = "FOCUS-based multi-cloud TCO spend-visualization platform"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "Yuga Chaparala", email = "ychaparala@gmail.com" }]
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
]
dependencies = [
    # Connector SDKs are imported eagerly by ingest, so they're required for now.
    # Making them optional extras needs lazy connector imports (follow-up).
    "boto3>=1.43.56",
    "databricks-sdk>=0.122.0",
    "duckdb>=1.5.5",
    "httpx>=0.28.1",
    "mcp>=1.28.1",
    "nicegui>=3.15.0",
    "pandas>=3.0.5",
    "platformdirs>=4.11.0",
    "plotly>=6.9.0",
    "pyarrow>=25.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.3",
    "structlog>=26.1.0",
    "typer>=0.27.0",
]

[project.optional-dependencies]
# Only needed for RedshiftConfig.bastion_host/db_password_env (a direct SQL
# connection, tunneled or not, for clusters not reachable via the Data API) —
# lazily imported so the default `pip install getflashlight` doesn't pull these
# in for everyone.
redshift-bastion = [
    "sshtunnel>=0.4.0",
    "redshift-connector>=2.1.15",
]

[project.scripts]
flashlight = "flashlight.cli:app"
fl = "flashlight.cli:app"

[project.urls]
Homepage = "https://getflashlight.app/"
Repository = "https://github.com/ychaparala/getflashlight"
Issues = "https://github.com/ychaparala/getflashlight/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flashlight"]

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pre-commit>=4.6.1",
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]
docs = [
    "mkdocs-material>=9.7.7",
    "mkdocs-llmstxt>=0.5.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
strict = true
ignore_missing_imports = true
